# Maintainer: Tales A. Mendonça <talesam@gmail.com>
#
# linux-big: the BigCommunity kernel.
#
# kernel.org sources, Manjaro's configuration and hardware patches, and on top:
#   - the BORE scheduler (Masahito Suzuki, firelzrd/bore-scheduler)
#   - the ADIOS I/O scheduler (Masahito Suzuki, firelzrd/adios)
#   - the POC idle CPU selector (Masahito Suzuki, firelzrd/poc-selector)
#   - BigCommunity's Intel Xe fixes, sent upstream
#   - a Clang/LLVM build with ThinLTO, ready for AutoFDO
#
# Everything the kernel changes is listed in prepare(), where it is also
# checked, so a new release that silently drops an option fails the build
# instead of shipping without it.
#
# Do not reference the pkgname or url variables anywhere in this file, not
# even in a comment: a testing build in the CI expands them textually, and in
# a split package pkgname expands to its first name only.

_basekernel=7.2
pkgbase=linux-big
pkgver=7.2.7
pkgrel=2
arch=('x86_64')
url="https://github.com/big-comm/big-kernel"
license=(GPL-2.0-only)
makedepends=(
  bc
  binutils
  clang
  cpio
  gettext
  glibc
  libelf
  libgcc
  lld
  llvm
  openssl
  pahole
  perl
  python
  rust
  rust-bindgen
  rust-src
  tar
  xxhash
  xz
  zlib
  zstd
)
# Gate, CI only: check() builds the NVIDIA open driver against this kernel,
# so a kernel that would leave NVIDIA users without their driver is never
# published. Not for a local build, where it would install the NVIDIA driver
# on the builder's own machine.
if [[ -n ${CI:-} || -n ${GITHUB_ACTIONS:-} ]]; then
  checkdepends=(nvidia-open-dkms)
fi

# The kernel handles its own optimisation and LTO; makepkg must not add its own.
options=(
  !debug
  !lto
  !strip
)
source=(https://cdn.kernel.org/pub/linux/kernel/v7.x/linux-${_basekernel}.tar.xz
        https://cdn.kernel.org/pub/linux/kernel/v7.x/patch-${pkgver}.xz
        config
        # Manjaro: security and hardware fixes
        0001-add-sysctl-to-disallow-unprivileged-CLONE_NEWUSER-by.patch
        0002-drivers-firmware-skip-simpledrm-if-nvidia-drm.modese.patch
        0000-drm-amdgpu-fix-race-condition-in-amdgpu_vm_wait_idle-during-process-kill.patch
        0001-drm-amd-Sanity-check-custom-brightness-curve-data-po.patch
        0000-usb-vhci-hcd-Unconditionally-allow-system-suspend.patch
        0999-patch_realtek.patch
        # Manjaro: handhelds (ASUS ROG Ally, Zotac Zone, Steam Deck, OrangePi NEO)
        0001-FROM-ML-platform-x86-asus-armoury-gate-PPT-writes-be.patch
        0002-FOR-UPSTREAM-hid-asus-ally-Add-joystick-LED-ring-sup.patch
        0003-FOR-UPSTREAM-hid-asus-ally-do-MCY-FW-validation-in-h.patch
        0004-FOR-UPSTREAM-hid-asus-ally-initial-Ally-X-gamepad-br.patch
        0005-FOR-UPSTREAM-hid-asus-ally-initial-gamepad-configura.patch
        0006-FOR-UPSTREAM-hid-asus-ally-add-button-remap-attribut.patch
        0007-FOR-UPSTREAM-hid-asus-ally-add-gamepad-mode-selectio.patch
        0008-FOR-UPSTREAM-hid-asus-ally-Turbo-settings-for-button.patch
        0009-FOR-UPSTREAM-hid-asus-ally-add-vibration-intensity-s.patch
        0010-FOR-UPSTREAM-hid-asus-ally-add-JS-deadzones.patch
        0011-FOR-UPSTREAM-hid-asus-ally-add-trigger-deadzones.patch
        0012-FOR-UPSTREAM-hid-asus-ally-add-anti-deadzones.patch
        0013-FOR-UPSTREAM-hid-asus-ally-add-JS-response-curves.patch
        0014-FOR-UPSTREAM-hid-asus-ally-mcu_version-attribute.patch
        0015-FOR-UPSTREAM-hid-asus-ally-add-calibrations-wip.patch
        0016-FOR-UPSTREAM-debug-by-default.patch
        0017-FOR-UPSTREAM-hid-asus-ally-grab-short-press-QAM-on-R.patch
        0018-FOR-UPSTREAM-hid-asus-ally-disable-wakeup-attribute-.patch
        0020-MAINTAINED-EXTERNALLY-hid-asus-ally-Separate-trigger.patch
        0021-MAINTAINED-EXTERNALLY-hid-asus-ally-add-sleep_animat.patch
        0001-iio-imu-bmi270-Match-PNP-ID-found-on-newer-OrangePi-NEO-firmware.patch
        0001-zotac-zone-hid-initial-impl.patch
        0002-xpad-gate-the-zotac-zone-PID-behind-if-IS_REACHABLE-.patch
        0003-tmp-apply-zotac-screen-quirk.patch
        0005-zone-fix-6.15-rename-del_timer-to-timer_delete.patch
        0001-steam-deck.patch
        0000-iwlwifi-fix.patch
        # BigCommunity: Intel display and Xe fixes
        0001-drm-i915-vrr-Check-TRANS_PUSH_SEND-after-DSB-commit.patch
        0002-drm-xe-mcr-Wait-for-steering-semaphore-before-lock.patch
        0003-drm-xe-mcr-Sanitize-steer-semaphore-on-resume.patch
        0010-brost-scope-lnl-flush-wa.patch
        # Schedulers
        0200-sched-bore-6.8.0.patch
        0201-block-adios-3.3.0r2.patch
        0202-sched-poc-selector-v2.6.3.patch
        # BigCommunity: Wa_22016122933 series, reviewed upstream (v6)
        0101-drm-xe-Capture-devcoredump-on-TLB-invalidation-timeo.patch
        0102-drm-xe-Log-when-a-timed-out-TLB-invalidation-ack-fin.patch
        0103-drm-xe-Implement-Wa_22016122933.patch
)
sha256sums=('f9fef3d14c0df53819026f4be74459835c2a0b0dcbf5b5bbd9ea19f0829402b3'
            '7d08d70146f0c81bfd529d97450044d356e774a93ae2ec041e7cd825f48b0695'
            '9b167034277170e1311a03884cd69fd94f09b116ce0f0f7451f3617be2e5fd7c'
            'e5e98d62b63704cecdf32dbe6a9bafea6e70b23fa8e01fe96ca220ac6036392e'
            'c21170eba77438abb8b8ab02aeccf16bfb2467a01303509945aa6b3a0fd16d31'
            '37f3222fafbe67dec3740933be37867e0c378468f71e9a6d5d6a07c2a2a568fe'
            'cacb08b2f43a9fd09053bffaacc4b7bdf8381772f26e61825fb696ded100af57'
            '512032c6b93fce24254da6cace7bf101c8f7c824761a0f99deed4b7724ac6f3e'
            '432c4e2f750d09b255024d547bdf5b014862ab297083ea04c62869db90d920ee'
            '4f186fa06bfb0e0e08320cb53a38afebe6d9eaa5c820396015c59f993bf8e1b6'
            '694934e1259e456715ee1358cd3fe934853a8818e9ea5fac2d5136aa6993b08f'
            '85689db7f888c5fba374b1fcc803e3e26ffc927fdef4d87a022ae9f275635449'
            '91bdd01e6a8ec2de0ca198c0a1916fb588e801abfb28cbdd7e745e7a5e2c9492'
            'afb9cee8496ce87587865c157dfc8966b387a62b611cf49a3b12b967410f2d05')

_srcdir="linux-${_basekernel}"

# The whole kernel is built with Clang/LLVM; it is what ThinLTO needs.
_llvm=(LLVM=1 LLVM_IAS=1)

# AutoFDO profile for this kernel series, collected with perf on a running
# linux-big (see the README). When set, the file must also be listed in
# source; the kernel is then optimised with it. Without one the kernel is
# still built with CONFIG_AUTOFDO_CLANG, ready to be profiled.
_autofdo_profile=

export KBUILD_BUILD_HOST=bigcommunity
export KBUILD_BUILD_USER=$pkgbase
export KBUILD_BUILD_TIMESTAMP="$(date -Ru${SOURCE_DATE_EPOCH:+d @$SOURCE_DATE_EPOCH})"

# Options this kernel exists for. prepare() fails if any of them did not
# survive olddefconfig, which drops an option silently when a dependency moves.
_required_options=(
  CONFIG_LOCALVERSION='"-big"'
  CONFIG_CC_IS_CLANG=y
  CONFIG_LTO_CLANG_THIN=y
  CONFIG_AUTOFDO_CLANG=y
  CONFIG_SCHED_BORE=y
  CONFIG_SCHED_POC_SELECTOR=y
  CONFIG_MQ_IOSCHED_ADIOS=y
  CONFIG_MQ_IOSCHED_DEFAULT_ADIOS=y
  CONFIG_HZ_1000=y
  CONFIG_PREEMPT_DYNAMIC=y
  CONFIG_SCHED_CLASS_EXT=y
  CONFIG_NTSYNC=m
  CONFIG_DEBUG_INFO_BTF=y
)

prepare() {
  cd $_srcdir

  echo "Setting version..."
  echo "-$pkgrel" > localversion.10-pkgrel

  echo "Applying upstream stable patch..."
  patch -p1 -i "../patch-${pkgver}"

  local src
  for src in "${source[@]}"; do
    src="${src%%::*}"
    src="${src##*/}"
    src="${src%.zst}"
    [[ $src = *.patch ]] || continue
    echo "Applying patch $src..."
    patch -Np1 < "../$src"
  done

  echo "Setting config..."
  cp ../config .config
  # The config is kept fully resolved. A generic x86-64 target is required:
  # the package runs on every user's CPU, not on the one that built it.
  scripts/config -d X86_NATIVE_CPU
  make "${_llvm[@]}" olddefconfig
  diff -u ../config .config || :

  local option missing=()
  for option in "${_required_options[@]}"; do
    grep -qxF "$option" .config || missing+=("$option")
  done
  if (( ${#missing[@]} )); then
    error "These options did not survive the configuration: ${missing[*]}"
    return 1
  fi

  make -s "${_llvm[@]}" kernelrelease > version
  msg "Prepared $pkgbase version $(<version)"
}

build() {
  cd $_srcdir
  local autofdo=()
  if [[ -n $_autofdo_profile ]]; then
    msg "Optimising with the AutoFDO profile $_autofdo_profile"
    autofdo=(CLANG_AUTOFDO_PROFILE="$srcdir/$_autofdo_profile")
  fi
  make "${_llvm[@]}" "${autofdo[@]}" ${MAKEFLAGS} bzImage modules
  make -C tools/bpf/bpftool "${_llvm[@]}" vmlinux.h feature-clang-bpf-co-re=1
}

check() {
  local nvidia
  nvidia=$(find /usr/src -maxdepth 1 -type d -name 'nvidia-*' | sort -V | tail -1)
  if [[ -z $nvidia ]]; then
    msg "NVIDIA gate skipped: no nvidia-open-dkms source (local build)"
    return 0
  fi

  msg "NVIDIA gate: building $(basename "$nvidia") against this kernel"
  rm -rf "$srcdir/nvidia-gate"
  cp -a "$nvidia" "$srcdir/nvidia-gate"
  cd "$srcdir/nvidia-gate"
  # The toolchain dkms uses for a Clang-built kernel. makepkg exports its own
  # CFLAGS and LDFLAGS, which the NVIDIA build hands to ld.lld as is, and
  # ld.lld rejects the compiler-driver ones (-Wl,-O1, ...): clear them.
  CFLAGS= CXXFLAGS= LDFLAGS= CC=clang LD=ld.lld \
    make "${_llvm[@]}" ${MAKEFLAGS} IGNORE_PREEMPT_RT_PRESENCE=1 \
    SYSSRC="$srcdir/$_srcdir" SYSOUT="$srcdir/$_srcdir" modules

  local module
  for module in nvidia nvidia-modeset nvidia-drm nvidia-uvm; do
    if [[ ! -f kernel-open/$module.ko ]]; then
      error "NVIDIA gate: $module.ko was not built"
      return 1
    fi
  done
  msg "NVIDIA gate passed"
}

_package() {
  pkgdesc="The BigCommunity kernel and modules: BORE, ADIOS, POC, Clang ThinLTO and Intel Xe fixes"
  depends=(
    'coreutils'
    'initramfs'
    'kmod'
  )
  optdepends=(
    'linux-firmware: firmware images needed for some devices'
    'scx-scheds: to use sched-ext schedulers'
    'wireless-regdb: to set the correct wireless channels of your country'
  )
  provides=(
    KSMBD-MODULE
    NTSYNC-MODULE
    VIRTUALBOX-GUEST-MODULES
    WIREGUARD-MODULE
  )

  cd $_srcdir
  local modulesdir="$pkgdir/usr/lib/modules/$(<version)"

  echo "Installing boot image..."
  # systemd expects to find the kernel here to allow hibernation
  # https://github.com/systemd/systemd/commit/edda44605f06a41fb86b7ab8128dcf99161d2344
  install -Dm644 "$(make -s image_name)" "$modulesdir/vmlinuz"

  # Used by mkinitcpio to name the kernel: /boot/vmlinuz-linux-big.
  # No kernelbase file on purpose -- Manjaro's hook would then name it
  # vmlinuz-7.2-x86_64 and overwrite the Manjaro kernel of the same series.
  echo "$pkgbase" | install -Dm644 /dev/stdin "$modulesdir/pkgbase"
  mkdir -p "${pkgdir}/boot"
  echo "$(<version) x64" > "${pkgdir}/boot/${pkgbase}-${CARCH}.kver"

  echo "Installing modules..."
  ZSTD_CLEVEL=19 make "${_llvm[@]}" INSTALL_MOD_PATH="$pkgdir/usr" INSTALL_MOD_STRIP=1 \
    DEPMOD=/doesnt/exist modules_install  # Suppress depmod

  # remove build link
  rm "$modulesdir"/build

  # now we call depmod...
  depmod -b "${pkgdir}/usr" -F System.map "$(<version)"
}

_package-headers() {
  pkgdesc="Headers and scripts for building modules for the BigCommunity kernel"
  # Modules for this kernel have to be built with the same Clang/LLVM.
  depends=(
    binutils
    clang
    glibc
    libelf
    libgcc
    lld
    llvm
    openssl
    pahole
    xxhash
    zlib
    zstd
  )
  # linux-headers is what packages such as bigcam depend on by name.
  provides=('LINUX-HEADERS' "linux-headers=$pkgver")

  cd $_srcdir
  local builddir="$pkgdir/usr/lib/modules/$(<version)/build"

  echo "Installing build files..."
  install -Dt "$builddir" -m644 .config Makefile Module.symvers System.map \
    localversion.* version vmlinux tools/bpf/bpftool/vmlinux.h
  install -Dt "$builddir/kernel" -m644 kernel/Makefile
  install -Dt "$builddir/arch/x86" -m644 arch/x86/Makefile
  cp -t "$builddir" -a scripts
  ln -srt "$builddir" "$builddir/scripts/gdb/vmlinux-gdb.py"

  # External modules (DKMS: NVIDIA, VirtualBox...) are built through this
  # Makefile. Default them to the LLVM toolchain the kernel was built with,
  # otherwise Kbuild calls GCC and fails on the Clang and LTO flags. An
  # explicit LLVM= on the command line still overrides it.
  sed -i '0,/^ifneq ($(LLVM),)/s//# linux-big: built with Clang\/LLVM, so are its external modules.\nLLVM ?= 1\nLLVM_IAS ?= 1\n\n&/' \
    "$builddir/Makefile"
  grep -q '^LLVM ?= 1$' "$builddir/Makefile"

  # required when STACK_VALIDATION is enabled
  install -Dt "$builddir/tools/objtool" tools/objtool/objtool

  # required when DEBUG_INFO_BTF_MODULES is enabled
  install -Dt "$builddir/tools/bpf/resolve_btfids" tools/bpf/resolve_btfids/resolve_btfids

  echo "Installing headers..."
  cp -t "$builddir" -a include
  cp -t "$builddir/arch/x86" -a arch/x86/include
  install -Dt "$builddir/arch/x86/kernel" -m644 arch/x86/kernel/asm-offsets.s

  install -Dt "$builddir/drivers/md" -m644 drivers/md/*.h
  install -Dt "$builddir/net/mac80211" -m644 net/mac80211/*.h

  # https://bugs.archlinux.org/task/13146
  install -Dt "$builddir/drivers/media/i2c" -m644 drivers/media/i2c/msp3400-driver.h

  # https://bugs.archlinux.org/task/20402
  install -Dt "$builddir/drivers/media/usb/dvb-usb" -m644 drivers/media/usb/dvb-usb/*.h
  install -Dt "$builddir/drivers/media/dvb-frontends" -m644 drivers/media/dvb-frontends/*.h
  install -Dt "$builddir/drivers/media/tuners" -m644 drivers/media/tuners/*.h

  # https://bugs.archlinux.org/task/71392
  install -Dt "$builddir/drivers/iio/common/hid-sensors" -m644 drivers/iio/common/hid-sensors/*.h

  echo "Installing KConfig files..."
  find . -name 'Kconfig*' -exec install -Dm644 {} "$builddir/{}" \;

  echo "Removing unneeded architectures..."
  local arch
  for arch in "$builddir"/arch/*/; do
    [[ $arch = */x86/ ]] && continue
    echo "Removing $(basename "$arch")"
    rm -r "$arch"
  done

  echo "Removing documentation..."
  rm -r "$builddir/Documentation"

  echo "Removing broken symlinks..."
  find -L "$builddir" -type l -printf 'Removing %P\n' -delete

  echo "Removing loose objects..."
  find "$builddir" -type f -name '*.o' -printf 'Removing %P\n' -delete

  echo "Stripping build tools..."
  local file
  while read -rd '' file; do
    case "$(file -Sib "$file")" in
      application/x-sharedlib\;*)      # Libraries (.so)
        strip -v $STRIP_SHARED "$file" ;;
      application/x-archive\;*)        # Libraries (.a)
        strip -v $STRIP_STATIC "$file" ;;
      application/x-executable\;*)     # Binaries
        strip -v $STRIP_BINARIES "$file" ;;
      application/x-pie-executable\;*) # Relocatable binaries
        strip -v $STRIP_SHARED "$file" ;;
    esac
  done < <(find "$builddir" -type f -perm -u+x ! -name vmlinux -print0)

  echo "Stripping vmlinux..."
  strip -v $STRIP_STATIC "$builddir/vmlinux"

  echo "Adding symlink..."
  mkdir -p "$pkgdir/usr/src"
  ln -sr "$builddir" "$pkgdir/usr/src/$pkgbase"
}

pkgname=(
  "$pkgbase"
  "$pkgbase-headers"
)
for _p in "${pkgname[@]}"; do
  eval "package_$_p() {
    $(declare -f "_package${_p#$pkgbase}")
    _package${_p#$pkgbase}
  }"
done

# vim:set ts=8 sts=2 sw=2 et:
